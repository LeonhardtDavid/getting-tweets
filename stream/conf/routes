# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET           /streams            controllers.Streams.find(interest: List[String])
POST          /streams            controllers.Streams.create
PUT           /streams            controllers.Streams.add
DELETE        /streams            controllers.Streams.delete
GET           /streams/run        controllers.Streams.run
